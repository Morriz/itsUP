__pycache__
*.bak*
.coverage
.env
.history
.*_cache
.venv
*.egg-info/
/data/
/htmlcov/
/proxy/
/reports/
/upstream/
/db.yml

# Independent git repos (not submodules)
/projects/
/secrets/

# Local AI config artifacts
.antigravitycli/
.claude/
.codex/
.gemini/

# TeleClaude stuff (todos DB, session caches, etc.)
.teleclaude/
**/index.yaml
# Inflated per-domain baselines generated beside their source manifest and
# exposed via the whole-tree docs symlink at ~/.teleclaude/docs/<domain>/.
docs/global/*/domain-baseline.md

# Generated tool output (never tracked)
graphify-out/

# Generated agent instructions — source of truth is AGENTS.master.md; the
# inflated AGENTS.md/CLAUDE.md build artifacts are never tracked.
AGENTS.md
CLAUDE.md

# Integration/worker git worktrees (operational, never tracked)
/trees/

*.local.json
itsup.tar.gz
