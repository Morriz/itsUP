---
version: '3.8'

services:
  api:
    image: morriz/doup:main
    ports:
      - 8888:8080
    restart: unless-stopped
    volumes:
      - ./bin:/app/bin
      - ./certs:/app/certs
      - ./hostpipe:/app/hostpipe
      - ./hostpipe.txt:/app/hostpipe.txt
      - ./data:/app/data
      - ./proxy:/app/proxy
      - ./upstream:/app/upstream
    env_file:
      - .env
